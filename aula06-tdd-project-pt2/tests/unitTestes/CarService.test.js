const { describe, it,  } = require('mocha');
const assert = require('assert');
const request = require('supertest');

describe('Car Services Suite Tests', () => {
        it('', async() => {
           const string = 'ok'
            assert.deepStrictEqual(string, 'ok')
         })
})