import {
    expect,
    describe,
    test,
    jest,
    beforeEach
} from '@jest/globals'

describe('#Util - Strings', () => {
   
    beforeEach(() => {
        jest.clearAllMocks()
        jest.restoreAllMocks()
    })

    test.todo('#upperCaseFirstLetter should transform the first letter in uppercase')
    test.todo('#lowerCaseFirstLetter should transform the first letter in lowercase')
    test.todo('#lowerCaseFirstLetter given an empty string should return empty')
    test.todo('#upperCaseFirstLetter given an empty string should return empty')
})